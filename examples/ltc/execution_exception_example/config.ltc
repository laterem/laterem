>formula = '(!A&B) | (C|!B)'
available_op_1 = 'NOT'
available_op_2 = 'OR'

test = BooleanFormulaOperators(answer)

a = Divide(42, 0)

answer?IsBooleanIdentical(formula)
test?Equal([available_op_1, available_op_2])

# Ответ: (C|!A|!B)